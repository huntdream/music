import React from 'react';
import SearchBar from '../../components/SearchBar';

interface Props {}

const Main: React.FC<Props> = () => {
  return (
    <div>
      <SearchBar />
    </div>
  );
};

export default Main;
